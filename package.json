{
  "name": "cms-core",
  "version": "0.2.0",
  "description": "Drupal-inspired CMS in TypeScript. Zero runtime dependencies in the framework core. Flat-file storage, hook-based modules, ~95% Drupal CMS parity.",
  "engines": {
    "node": ">=22.0.0"
  },
  "type": "module",
  "main": "index.ts",
  "scripts": {
    "prepare": "lefthook install || true",
    "start": "node server.js",
    "start:raw": "node index.js",
    "build": "node build.js",
    "build:watch": "node build.js --watch",
    "typecheck": "tsc",
    "lint": "biome lint .",
    "lint:fix": "biome lint --write .",
    "format": "biome format --write .",
    "test": "node --test --test-reporter=spec tests/unit/*.js tests/integration/*.js",
    "test:watch": "node --test --watch tests/unit/*.js tests/integration/*.js",
    "check:size": "node --experimental-strip-types scripts/check-file-size.ts",
    "check:layers": "node --experimental-strip-types scripts/check-layer-boundaries.ts",
    "check:drift": "node --experimental-strip-types scripts/check-schema-drift.ts",
    "restructure": "node --experimental-strip-types scripts/restructure.ts",
    "check": "npm run typecheck && npm run lint && npm test && npm run check:size && npm run check:layers && npm run check:drift"
  },
  "keywords": [
    "cms",
    "modular",
    "extensible"
  ],
  "license": "MIT",
  "dependencies": {
    "@tiptap/core": "^2.11.0",
    "@tiptap/extension-code-block-lowlight": "^2.11.0",
    "@tiptap/extension-image": "^2.11.0",
    "@tiptap/extension-link": "^2.11.0",
    "@tiptap/extension-placeholder": "^2.11.0",
    "@tiptap/extension-table": "^2.11.0",
    "@tiptap/extension-table-cell": "^2.11.0",
    "@tiptap/extension-table-header": "^2.11.0",
    "@tiptap/extension-table-row": "^2.11.0",
    "@tiptap/extension-text-align": "^2.11.0",
    "@tiptap/extension-underline": "^2.11.0",
    "@tiptap/pm": "^2.11.0",
    "@tiptap/starter-kit": "^2.11.0",
    "sharp": "^0.33.0"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.12",
    "@types/node": "^22.19.11",
    "esbuild": "^0.24.0",
    "lefthook": "^2.1.6",
    "ts-morph": "^28.0.0",
    "typescript": "^5.9.3"
  }
}
